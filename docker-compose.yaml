services:
  db:
    image: postgres
    container_name: devops_db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=devops
      - POSTGRES_USER=devops
      - POSTGRES_PASSWORD=devops